@evolang "3.0"

@xiangci {
    "LSP 语言服务器入口 — 为 .evo 语言提供语法高亮/补全/诊断"
}

@locus lsp.entry {
    mut_rate   = 0.005
    cross_pool = "tools"
    fitness    = min_latency
    env_target = ["linux-6.x"]

    卦序: {
        ䷁ RECV R0, 0
        ䷾ SYNC
        ䷋ HALT
    }
}
